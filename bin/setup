#!/bin/bash -eu

export PATH=/opt/rh/ruby193/root/bin:$PATH
export LD_LIBRARY_PATH=/opt/rh/ruby193/root/usr/lib64
cd $OPENSHIFT_JEKYLL_DIR && bundle install
mkdir $OPENSHIFT_JEKYLL_DIR/logs
mkdir $OPENSHIFT_JEKYLL_DIR/.gems

